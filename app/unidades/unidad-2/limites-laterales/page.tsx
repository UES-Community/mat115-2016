'use client';

import Link from 'next/link';
import { useState } from 'react';
import { ArrowLeft, CheckCircle2, ChevronsLeftRight, XCircle } from 'lucide-react';
import MathRender from '@/components/MathRender';

export default function OneSidedLimitsPage() {
  const [side, setSide] = useState<'left' | 'right'>('left');
  const [answer, setAnswer] = useState<boolean | null>(null);
  const value = side === 'left' ? 2 : 3;

  return (
    <div className="mx-auto max-w-[1200px] space-y-10 px-4 py-10 sm:py-16"><div className="space-y-5"><Link href="/unidades/unidad-2" className="inline-flex items-center gap-2 text-sm text-ash transition-colors hover:text-quartz"><ArrowLeft className="h-4 w-4" />Volver a la guía de Unidad 2</Link><div className="flex items-start gap-4"><div className="flex h-12 w-12 shrink-0 items-center justify-center rounded-xl border border-sapphire-hairline bg-cobalt-panel text-signal-blue"><ChevronsLeftRight className="h-6 w-6" /></div><div className="space-y-2"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-frosted-lilac">Unidad 2 · Tema 02</p><h1 className="font-figtree text-3xl font-medium tracking-tight text-quartz sm:text-5xl">Límites laterales</h1><p className="max-w-3xl text-base leading-relaxed text-mist">Compara lo que ocurre cuando x se aproxima a un punto desde la izquierda y desde la derecha.</p></div></div></div><section className="grid gap-5 md:grid-cols-2"><article className="agentql-card space-y-4"><p className="text-xs font-semibold uppercase tracking-wider text-frosted-lilac">Definición</p><MathRender math="\\lim_{x \\to a^-}f(x), \\quad \\lim_{x \\to a^+}f(x)" block /><p className="text-sm leading-relaxed text-mist">El límite bilateral existe solo cuando ambos límites laterales existen y son iguales.</p></article><article className="agentql-card space-y-4"><p className="text-xs font-semibold uppercase tracking-wider text-frosted-lilac">Ejemplo por tramos</p><MathRender math="f(x)=\\begin{cases}x+1 & x<1\\\\ x+2 & x>1\\end{cases}" block /><p className="text-sm leading-relaxed text-mist">Cerca de x = 1, la rama izquierda se acerca a 2 y la derecha a 3.</p></article></section><section className="agentql-highlight-card space-y-6"><div><p className="mb-2 text-xs font-semibold uppercase tracking-wider text-frosted-lilac">Comparador lateral</p><h2 className="font-figtree text-2xl text-quartz">Acércate a x = 1</h2></div><div className="flex flex-wrap gap-3"><button type="button" onClick={() => setSide('left')} className={`rounded-full border px-4 py-2 text-sm ${side === 'left' ? 'border-frosted-lilac bg-cobalt-panel text-quartz' : 'border-obsidian-edge text-mist'}`}>Desde la izquierda</button><button type="button" onClick={() => setSide('right')} className={`rounded-full border px-4 py-2 text-sm ${side === 'right' ? 'border-frosted-lilac bg-cobalt-panel text-quartz' : 'border-obsidian-edge text-mist'}`}>Desde la derecha</button></div><div className="rounded-lg border border-sapphire-hairline bg-cobalt-panel/60 p-4 font-mono text-2xl text-frosted-lilac">Límite {side === 'left' ? 'izquierdo' : 'derecho'} = {value}</div></section><section className="agentql-card space-y-5"><div><p className="mb-2 text-xs font-semibold uppercase tracking-wider text-frosted-lilac">Autoevaluación</p><h2 className="font-figtree text-xl text-quartz">¿Existe el límite bilateral de este ejemplo en x = 1?</h2></div><div className="flex gap-3">{[true, false].map((v) => <button key={String(v)} type="button" onClick={() => setAnswer(v)} className={`rounded-full border px-5 py-2 text-sm ${answer === v ? 'border-frosted-lilac bg-cobalt-panel text-quartz' : 'border-obsidian-edge text-mist'}`}>{v ? 'Sí' : 'No'}</button>)}</div>{answer !== null && <div className={`flex items-start gap-3 text-sm ${answer === false ? 'text-emerald-200' : 'text-pink-200'}`} role="status">{answer === false ? <CheckCircle2 className="h-5 w-5" /> : <XCircle className="h-5 w-5" />}<p>{answer === false ? 'Correcto: 2 ≠ 3, por lo tanto no existe un límite bilateral único.' : 'Compara ambos valores laterales: al ser diferentes, el límite bilateral no existe.'}</p></div>}</section></div>
  );
}
